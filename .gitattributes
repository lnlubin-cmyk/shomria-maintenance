# Normalize line endings to LF in the repository. Windows checkouts still get
# CRLF locally; without this the repo's contents depend on who committed.
* text=auto eol=lf

*.png binary
*.jpg binary
*.ico binary
